// Code generated by go generate via internal/cmd/service; DO NOT EDIT.
package {{ .Name }}

import (
"context"
"io"

"github.com/opentracing/opentracing-go"

"github.com/Xuanwo/storage"
"github.com/Xuanwo/storage/pkg/segment"
"github.com/Xuanwo/storage/pkg/endpoint"
"github.com/Xuanwo/storage/pkg/credential"
"github.com/Xuanwo/storage/pkg/storageclass"
"github.com/Xuanwo/storage/pkg/httpclient"
"github.com/Xuanwo/storage/services"
"github.com/Xuanwo/storage/types"
ps "github.com/Xuanwo/storage/types/pairs"
"github.com/Xuanwo/storage/types/metadata"
)

{{- $Data := . }}

var _ credential.Provider
var _ endpoint.Provider
var _ segment.Segment
var _ storage.Storager
var _ storageclass.Type
var _ services.ServiceError
var _ httpclient.Options

// Type is the type for {{ .Name }}
const Type = "{{ .Name }}"

{{- if .Service }}
    {{- template "pairs" makeSlice "service" .Service .TypeMap }}
    {{- template "context_func" makeSlice "service" .Service .Name }}
{{- end }}

{{- template "pairs" makeSlice "storage" .Storage .TypeMap }}
{{- template "context_func" makeSlice "storage" .Storage .Name }}

{{- define "pairs" }}
    {{- $pn := index . 0 | toPascal }}
    {{- $data := index . 1 }}
    {{- $typeMap := index . 2 }}

    {{- range $_, $v := $data.Op }}
        {{- $pk := $v.Op | toPascal }}
        var pair{{ $pn }}{{ $pk }}Map = map[string]struct{}{
        // Meta-defined pairs
        {{- range $v := $v.Required }}
            "{{ $v }}": struct{}{},
        {{- end }}
        {{- range $v := $v.Optional }}
            "{{ $v }}": struct{}{},
        {{- end }}
        }

        type pair{{ $pn }}{{ $pk }} struct {
        // Meta-defined pairs
        {{- range $_, $key := $v.Required }}
            {{ $key | toPascal }}    {{ (index $typeMap $key) }}
        {{- end }}
        {{- range $_, $key := $v.Optional }}
            Has{{ $key | toPascal }} bool
            {{ $key | toPascal }}    {{ (index $typeMap $key) }}
        {{- end }}
        }

        {{ if hasPrefix $pk "New" -}}
            func parse{{ $pn }}Pair
        {{- else -}}
            func (s *{{ $pn }} ) parsePair
        {{- end -}}
        {{ $pk }}(opts ...*types.Pair) (*pair{{ $pn }}{{ $pk }}, error) {
        result := &pair{{ $pn }}{{ $pk }}{}

        values := make(map[string]interface{})
        for _, v := range opts {
        {{- if not (hasPrefix $pk "New") }}
            if _, ok := pair{{ $pn }}{{ $pk }}Map[v.Key]; !ok {
            return nil, services.NewPairUnsupportedError(v)
            }
        {{- end }}
        values[v.Key] = v.Value
        }

        var v interface{}
        var ok bool

        // Parse meta-defined pairs
        {{- range $_, $key := $v.Required }}
            {{- $pkey := $key | toPascal }}
            v, ok = values[ps.{{ $pkey }}]
            if !ok {
                return nil, services.NewPairRequiredError(ps.{{ $pkey }})
            }
            if ok {
                result.{{ $pkey }} = v.({{ (index $typeMap $key) }})
            }
        {{- end }}
        {{- range $_, $key := $v.Optional }}
            {{- $pkey := $key | toPascal }}
            v, ok = values[ps.{{ $pkey }}]
            if ok {
            result.Has{{ $pkey }} = true
            result.{{ $pkey }} = v.({{ index $typeMap $key }})
            }
        {{- end }}

        return result, nil
        }
    {{- end }}
{{- end}}

{{- define "context_func" }}
    {{- $name := index . 0}}
    {{- $pn := $name | toPascal }}
    {{- $data := index . 1 }}
    {{- $serviceName := index . 2}}
    {{- range $_, $v := $data.Op }}
        {{ $fn := $v.Func }}
        {{- if $fn }}
            {{- if $fn.HasContext }}
                // {{ $fn.Parent }}WithContext adds context support for {{ $fn.Parent }}.
                func ({{ $fn.Receiver }}) {{ $fn.Parent }}WithContext(ctx context.Context, {{ $fn.Params }}) ( {{ $fn.Returns }}) {
                span, ctx := opentracing.StartSpanFromContext(ctx, "github.com/Xuanwo/storage/services/{{ $serviceName }}.{{ $name }}.{{ $fn.Parent }}")
                defer span.Finish()

                pairs = append(pairs, ps.WithContext(ctx))
                return s.{{ $fn.Parent }}({{ $fn.Caller }})
                }
            {{- end }}
        {{- end }}
    {{- end }}
{{- end }}